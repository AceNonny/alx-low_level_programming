#include "main.h"

/**
 * mul - multiplies two digits
 * @a: the first integer to be multiplied
 * @b: the second integer that multiplies the first integer
 * Return: The product of the multiplication of a and b
*/

int mul(int a, int b)
{
	return (a * y);
}
